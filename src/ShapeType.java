/**
 * This enum represents the types of shapes that can be created.
 * It is used in the Shape class and its subclasses to specify the type of the shape.
 */
public enum ShapeType {
    RECTANGLE, // Represents a rectangle shape
    TRIANGLE,  // Represents a triangle shape
    SQUARE     // Represents a square shape
}